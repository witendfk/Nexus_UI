# Nexus UI MVP 需求文档

## 1. 产品定位

Nexus UI 是面向 Agent 开发者的 A2UI Runtime。它让 Agent 输出声明式 UI 协议，而不是 HTML 或前端代码；客户端负责校验、渐进渲染和交互回流。

一句话目标：

> 让任何 Agent 都能安全地把结果输出为可交互 UI，并把用户操作回流到 Agent 或业务后端。

## 2. 目标用户

| 用户 | 价值 |
|---|---|
| Agent 开发者 | 不必为每个 Agent 临时实现 UI 协议、渲染器和交互回流 |
| Agent 平台 | 获得可校验的 UI 输出边界和统一运行时 |
| 业务系统 | 保留数据和权限，接入 Agent 生成的任务卡片或工作台 UI |

个人项目阶段的首要用户是开发者。Nexus UI 先成为可复用 runtime，再通过示例 Agent 验证落地，不提前绑定 CRM、客服或运维等垂直场景。

## 3. MVP 用户故事

作为一个 Agent 开发者，我希望：

1. 输入自然语言需求。
2. Agent 生成 A2UI v0.9 JSONL。
3. 服务端在下发前校验结构和生命周期。
4. 前端边接收边渲染。
5. 用户点击生成的按钮。
6. 前端把官方 action 消息回传服务端。
7. Agent 更新同一个 surface。
8. 界面原地变化，而不是重建整张卡片。

## 4. 功能需求

| 编号 | 需求 | MVP 状态 |
|---|---|---|
| FR-01 | 消费 A2UI v0.9 JSONL | 完成 |
| FR-02 | 信封、payload 和生命周期校验 | 完成 |
| FR-03 | 扁平组件表到 VNode 的渐进树构建 | 完成 |
| FR-04 | `{ path }` 数据绑定解析 | 完成 |
| FR-05 | React renderMap 渲染 | 完成，8 组件子集 |
| FR-06 | SSE 流式传输 | 完成 |
| FR-07 | LLM streaming 生成 | 完成 |
| FR-08 | 无 key fallback | 完成 |
| FR-09 | action 回流同 surface 更新 | 完成 |
| FR-10 | 服务端 Agent guard | 完成 |
| FR-11 | 自定义 Catalog Registry | 架构完成；端到端接入后续 |
| FR-12 | 表单与双向绑定 | 后续 |
| FR-13 | 多 surface 并发展示 | 后续 |
| FR-14 | 业务 action handler Adapter | 后续 |

## 5. 非目标

当前 MVP 明确不做：

- 生成 HTML、React 源码或在浏览器执行模型代码。
- 完整实现 Basic Catalog 的 18 个组件。
- 表单、checks、`sendDataModel`、ChildList template 和 FunctionCall。
- 多 surface、多框架、组件画廊、协议调试面板。
- 请求级 catalog 选择与端到端自定义 Catalog 样例。
- 替代业务后端的权限、数据和事务。
- 绑定某个垂直行业 Agent。

这些限制是为了保证第一条 Agent 线足够小、可测试、可验收。

## 6. 验收标准

### 6.1 真实 LLM 生成

- `/health` 返回 `agentMode: "llm"`。
- 输入非联系人卡片需求时，生成对应 UI。
- SSE 第一条业务消息是 `createSurface`。
- 后续消息只更新同一 surface。
- 至少一个 `updateComponents` 包含 `id=root`。
- 成功流以 `done` 结束。

### 6.2 Action 回流

- 点击 LLM 生成的 Button。
- 前端发送 v0.9 client-to-server action 信封。
- 服务端响应只包含 `updateComponents` / `updateDataModel`。
- `surfaceId` 保持不变。
- UI 原地更新，不 create/delete surface。

### 6.3 坏输出防线

以下输出必须被服务端拒绝，并通过 SSE `error` 返回，不下发 `done`：

- 非法 JSON。
- 错误 `version`。
- 一个信封包含多个 payload。
- 错误 `surfaceId`。
- 不支持组件。
- 生成流缺少 `root`。
- 生成流第一条不是 `createSurface`。
- action 响应尝试 `createSurface` / `deleteSurface`。

## 7. 里程碑

| 里程碑 | 交付 | 验收 |
|---|---|---|
| M0 | 工作区、类型与骨架 | 包可以独立 typecheck/build |
| M1 | core runtime | JSONL、状态、树构建、action 单元测试 |
| M2 | React renderer | renderMap 和 Provider 渲染测试 |
| M3 | SSE server | fallback 卡片端到端可用 |
| M4 | LLM Agent | 真实模型流式输出并通过 guard |
| M5 | 产品闭环 | 真实 LLM + action 原地更新 + 坏输出防线 |
| M6 | Catalog Registry 架构与端到端自定义 Catalog 样例 | M6-a 架构验收通过 |

M5 已完成：真实 LLM 手工与接口级验收通过，测试环境与项目 `.env` 隔离，全仓 `test / typecheck / lint / build` 通过。

M6-a 已完成：core 提供框架无关 `CatalogRegistry`；React 可按 surface `catalogId` 选择 renderMap；server Agent guard 可按注册表校验组件边界。全仓 `test / typecheck / lint / build` 已通过。M6-b 仍需完成请求级选择和端到端自定义组件验收。

## 8. 开发流程

每个功能必须按以下顺序推进：

1. **定义边界**：写清支持、不支持和验收标准。
2. **先补测试**：用最小用例锁住协议或行为。
3. **小步实现**：只改当前里程碑相关模块。
4. **局部验证**：先跑包级 test/typecheck。
5. **端到端验收**：Web 生成、action、原地更新。
6. **记录结果**：更新里程碑文档和当前状态。

只有当前步骤验收通过，才进入下一个能力扩展。
